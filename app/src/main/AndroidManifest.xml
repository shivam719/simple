<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.solution.simple">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".notifactiionpush"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Simple">
        <activity android:name=".Fregment.FragmentLayout"><intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".PushNotificationFirebaseActivity" />
        <activity android:name=".PushNotification">

        </activity>
        <activity android:name=".BubbleActivity" />
        <activity android:name=".RecyclerView" />
        <activity android:name=".Ere" />
        <activity android:name=".binding" />
        <activity android:name="com.Dashboard" />
        <activity android:name=".MainActivity" />

        <service
            android:name=".bubble_services"
            android:enabled="true" />
    </application>

</manifest>